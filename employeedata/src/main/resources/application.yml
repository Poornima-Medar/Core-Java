spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: test123
    url: jdbc:postgresql://localhost:5432/EmployeeDatabase
    username: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
